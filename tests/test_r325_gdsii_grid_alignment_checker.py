"""R325 GDSII 网"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / Grid"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
#"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str ="""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 G"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell_name: 顶层 cell 名。

    Returns:
        GDSII 文件路径。
    """
"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell_name: 顶层 cell 名。

    Returns:
        GDSII 文件路径。
    """
    import klayout.db as db

    ly = db.Layout()
    ly.dbu = db"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell_name: 顶层 cell 名。

    Returns:
        GDSII 文件路径。
    """
    import klayout.db as db

    ly = db.Layout()
    ly.dbu = dbu_um
    top = ly.create_cell(top_cell_name)
    for spec in box_specs:
"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell_name: 顶层 cell 名。

    Returns:
        GDSII 文件路径。
    """
    import klayout.db as db

    ly = db.Layout()
    ly.dbu = dbu_um
    top = ly.create_cell(top_cell_name)
    for spec in box_specs:
        layer = int(spec["layer"])
        datatype = int(spec["datatype"])
        li = ly"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell_name: 顶层 cell 名。

    Returns:
        GDSII 文件路径。
    """
    import klayout.db as db

    ly = db.Layout()
    ly.dbu = dbu_um
    top = ly.create_cell(top_cell_name)
    for spec in box_specs:
        layer = int(spec["layer"])
        datatype = int(spec["datatype"])
        li = ly.layer(layer, datatype)
        # μm → dbu（用 round 保持精度，与 grid 检查一致）
        left_dbu = int(round(spec["left"] / dbu_um))
        bottom_dbu = int(round(spec[""""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell_name: 顶层 cell 名。

    Returns:
        GDSII 文件路径。
    """
    import klayout.db as db

    ly = db.Layout()
    ly.dbu = dbu_um
    top = ly.create_cell(top_cell_name)
    for spec in box_specs:
        layer = int(spec["layer"])
        datatype = int(spec["datatype"])
        li = ly.layer(layer, datatype)
        # μm → dbu（用 round 保持精度，与 grid 检查一致）
        left_dbu = int(round(spec["left"] / dbu_um))
        bottom_dbu = int(round(spec["bottom"] / dbu_um))
        right_dbu = int(round(spec["right"] / dbu_um))
        top_dbu = int(round(spec["top"] / dbu_um))
        box = db.Box(left_dbu, bottom_db"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell_name: 顶层 cell 名。

    Returns:
        GDSII 文件路径。
    """
    import klayout.db as db

    ly = db.Layout()
    ly.dbu = dbu_um
    top = ly.create_cell(top_cell_name)
    for spec in box_specs:
        layer = int(spec["layer"])
        datatype = int(spec["datatype"])
        li = ly.layer(layer, datatype)
        # μm → dbu（用 round 保持精度，与 grid 检查一致）
        left_dbu = int(round(spec["left"] / dbu_um))
        bottom_dbu = int(round(spec["bottom"] / dbu_um))
        right_dbu = int(round(spec["right"] / dbu_um))
        top_dbu = int(round(spec["top"] / dbu_um))
        box = db.Box(left_dbu, bottom_dbu, right_dbu, top_dbu)
        top.shapes(li).insert(box)
    ly.write(str(output_path))
    return output_path


# =============================================================================
# 共享 fixtures
# =============================================================================
@pytest.fixture
def on_grid_polygon_gds(tmp_path: Path) -> Path:
    """创建所有顶点在 5nm grid 上的 polygon"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell_name: 顶层 cell 名。

    Returns:
        GDSII 文件路径。
    """
    import klayout.db as db

    ly = db.Layout()
    ly.dbu = dbu_um
    top = ly.create_cell(top_cell_name)
    for spec in box_specs:
        layer = int(spec["layer"])
        datatype = int(spec["datatype"])
        li = ly.layer(layer, datatype)
        # μm → dbu（用 round 保持精度，与 grid 检查一致）
        left_dbu = int(round(spec["left"] / dbu_um))
        bottom_dbu = int(round(spec["bottom"] / dbu_um))
        right_dbu = int(round(spec["right"] / dbu_um))
        top_dbu = int(round(spec["top"] / dbu_um))
        box = db.Box(left_dbu, bottom_dbu, right_dbu, top_dbu)
        top.shapes(li).insert(box)
    ly.write(str(output_path))
    return output_path


# =============================================================================
# 共享 fixtures
# =============================================================================
@pytest.fixture
def on_grid_polygon_gds(tmp_path: Path) -> Path:
    """创建所有顶点在 5nm grid 上的 polygon GDSII。

    矩形: ("""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell_name: 顶层 cell 名。

    Returns:
        GDSII 文件路径。
    """
    import klayout.db as db

    ly = db.Layout()
    ly.dbu = dbu_um
    top = ly.create_cell(top_cell_name)
    for spec in box_specs:
        layer = int(spec["layer"])
        datatype = int(spec["datatype"])
        li = ly.layer(layer, datatype)
        # μm → dbu（用 round 保持精度，与 grid 检查一致）
        left_dbu = int(round(spec["left"] / dbu_um))
        bottom_dbu = int(round(spec["bottom"] / dbu_um))
        right_dbu = int(round(spec["right"] / dbu_um))
        top_dbu = int(round(spec["top"] / dbu_um))
        box = db.Box(left_dbu, bottom_dbu, right_dbu, top_dbu)
        top.shapes(li).insert(box)
    ly.write(str(output_path))
    return output_path


# =============================================================================
# 共享 fixtures
# =============================================================================
@pytest.fixture
def on_grid_polygon_gds(tmp_path: Path) -> Path:
    """创建所有顶点在 5nm grid 上的 polygon GDSII。

    矩形: (0,0)-(10,5) μm，所有坐标是 5nm 的倍数（0, 5, 10...nm）。
    grid_um=0.005 → grid_dbu=5，应无违规。
    """
    cells_spec = [
        {
"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell_name: 顶层 cell 名。

    Returns:
        GDSII 文件路径。
    """
    import klayout.db as db

    ly = db.Layout()
    ly.dbu = dbu_um
    top = ly.create_cell(top_cell_name)
    for spec in box_specs:
        layer = int(spec["layer"])
        datatype = int(spec["datatype"])
        li = ly.layer(layer, datatype)
        # μm → dbu（用 round 保持精度，与 grid 检查一致）
        left_dbu = int(round(spec["left"] / dbu_um))
        bottom_dbu = int(round(spec["bottom"] / dbu_um))
        right_dbu = int(round(spec["right"] / dbu_um))
        top_dbu = int(round(spec["top"] / dbu_um))
        box = db.Box(left_dbu, bottom_dbu, right_dbu, top_dbu)
        top.shapes(li).insert(box)
    ly.write(str(output_path))
    return output_path


# =============================================================================
# 共享 fixtures
# =============================================================================
@pytest.fixture
def on_grid_polygon_gds(tmp_path: Path) -> Path:
    """创建所有顶点在 5nm grid 上的 polygon GDSII。

    矩形: (0,0)-(10,5) μm，所有坐标是 5nm 的倍数（0, 5, 10...nm）。
    grid_um=0.005 → grid_dbu=5，应无违规。
    """
    cells_spec = [
        {
            "name": "TOP",
            "polygons": [
                {
                    "layer":"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell_name: 顶层 cell 名。

    Returns:
        GDSII 文件路径。
    """
    import klayout.db as db

    ly = db.Layout()
    ly.dbu = dbu_um
    top = ly.create_cell(top_cell_name)
    for spec in box_specs:
        layer = int(spec["layer"])
        datatype = int(spec["datatype"])
        li = ly.layer(layer, datatype)
        # μm → dbu（用 round 保持精度，与 grid 检查一致）
        left_dbu = int(round(spec["left"] / dbu_um))
        bottom_dbu = int(round(spec["bottom"] / dbu_um))
        right_dbu = int(round(spec["right"] / dbu_um))
        top_dbu = int(round(spec["top"] / dbu_um))
        box = db.Box(left_dbu, bottom_dbu, right_dbu, top_dbu)
        top.shapes(li).insert(box)
    ly.write(str(output_path))
    return output_path


# =============================================================================
# 共享 fixtures
# =============================================================================
@pytest.fixture
def on_grid_polygon_gds(tmp_path: Path) -> Path:
    """创建所有顶点在 5nm grid 上的 polygon GDSII。

    矩形: (0,0)-(10,5) μm，所有坐标是 5nm 的倍数（0, 5, 10...nm）。
    grid_um=0.005 → grid_dbu=5，应无违规。
    """
    cells_spec = [
        {
            "name": "TOP",
            "polygons": [
                {
                    "layer": 1,
                    "datatype": 0,
"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell_name: 顶层 cell 名。

    Returns:
        GDSII 文件路径。
    """
    import klayout.db as db

    ly = db.Layout()
    ly.dbu = dbu_um
    top = ly.create_cell(top_cell_name)
    for spec in box_specs:
        layer = int(spec["layer"])
        datatype = int(spec["datatype"])
        li = ly.layer(layer, datatype)
        # μm → dbu（用 round 保持精度，与 grid 检查一致）
        left_dbu = int(round(spec["left"] / dbu_um))
        bottom_dbu = int(round(spec["bottom"] / dbu_um))
        right_dbu = int(round(spec["right"] / dbu_um))
        top_dbu = int(round(spec["top"] / dbu_um))
        box = db.Box(left_dbu, bottom_dbu, right_dbu, top_dbu)
        top.shapes(li).insert(box)
    ly.write(str(output_path))
    return output_path


# =============================================================================
# 共享 fixtures
# =============================================================================
@pytest.fixture
def on_grid_polygon_gds(tmp_path: Path) -> Path:
    """创建所有顶点在 5nm grid 上的 polygon GDSII。

    矩形: (0,0)-(10,5) μm，所有坐标是 5nm 的倍数（0, 5, 10...nm）。
    grid_um=0.005 → grid_dbu=5，应无违规。
    """
    cells_spec = [
        {
            "name": "TOP",
            "polygons": [
                {
                    "layer": 1,
                    "datatype": 0,
                    "points": [[0, 0], [10, 0], [10, 5], [0, 5]],
                },
            ],
            "is_top": True,
        }
    ]
    out = tmp_path / "on_grid.gds"
    export_gdsii_from_cells(cells_spec, out)
    return out


"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell_name: 顶层 cell 名。

    Returns:
        GDSII 文件路径。
    """
    import klayout.db as db

    ly = db.Layout()
    ly.dbu = dbu_um
    top = ly.create_cell(top_cell_name)
    for spec in box_specs:
        layer = int(spec["layer"])
        datatype = int(spec["datatype"])
        li = ly.layer(layer, datatype)
        # μm → dbu（用 round 保持精度，与 grid 检查一致）
        left_dbu = int(round(spec["left"] / dbu_um))
        bottom_dbu = int(round(spec["bottom"] / dbu_um))
        right_dbu = int(round(spec["right"] / dbu_um))
        top_dbu = int(round(spec["top"] / dbu_um))
        box = db.Box(left_dbu, bottom_dbu, right_dbu, top_dbu)
        top.shapes(li).insert(box)
    ly.write(str(output_path))
    return output_path


# =============================================================================
# 共享 fixtures
# =============================================================================
@pytest.fixture
def on_grid_polygon_gds(tmp_path: Path) -> Path:
    """创建所有顶点在 5nm grid 上的 polygon GDSII。

    矩形: (0,0)-(10,5) μm，所有坐标是 5nm 的倍数（0, 5, 10...nm）。
    grid_um=0.005 → grid_dbu=5，应无违规。
    """
    cells_spec = [
        {
            "name": "TOP",
            "polygons": [
                {
                    "layer": 1,
                    "datatype": 0,
                    "points": [[0, 0], [10, 0], [10, 5], [0, 5]],
                },
            ],
            "is_top": True,
        }
    ]
    out = tmp_path / "on_grid.gds"
    export_gdsii_from_cells(cells_spec, out)
    return out


@pytest.fixture
def off_grid_polygon_gds(tmp_path: Path) -> Path:
    """创建含 off-grid 顶点的 polygon GDSII"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell_name: 顶层 cell 名。

    Returns:
        GDSII 文件路径。
    """
    import klayout.db as db

    ly = db.Layout()
    ly.dbu = dbu_um
    top = ly.create_cell(top_cell_name)
    for spec in box_specs:
        layer = int(spec["layer"])
        datatype = int(spec["datatype"])
        li = ly.layer(layer, datatype)
        # μm → dbu（用 round 保持精度，与 grid 检查一致）
        left_dbu = int(round(spec["left"] / dbu_um))
        bottom_dbu = int(round(spec["bottom"] / dbu_um))
        right_dbu = int(round(spec["right"] / dbu_um))
        top_dbu = int(round(spec["top"] / dbu_um))
        box = db.Box(left_dbu, bottom_dbu, right_dbu, top_dbu)
        top.shapes(li).insert(box)
    ly.write(str(output_path))
    return output_path


# =============================================================================
# 共享 fixtures
# =============================================================================
@pytest.fixture
def on_grid_polygon_gds(tmp_path: Path) -> Path:
    """创建所有顶点在 5nm grid 上的 polygon GDSII。

    矩形: (0,0)-(10,5) μm，所有坐标是 5nm 的倍数（0, 5, 10...nm）。
    grid_um=0.005 → grid_dbu=5，应无违规。
    """
    cells_spec = [
        {
            "name": "TOP",
            "polygons": [
                {
                    "layer": 1,
                    "datatype": 0,
                    "points": [[0, 0], [10, 0], [10, 5], [0, 5]],
                },
            ],
            "is_top": True,
        }
    ]
    out = tmp_path / "on_grid.gds"
    export_gdsii_from_cells(cells_spec, out)
    return out


@pytest.fixture
def off_grid_polygon_gds(tmp_path: Path) -> Path:
    """创建含 off-grid 顶点的 polygon GDSII。

    矩形顶点: (0,0), (0.007,0),"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell_name: 顶层 cell 名。

    Returns:
        GDSII 文件路径。
    """
    import klayout.db as db

    ly = db.Layout()
    ly.dbu = dbu_um
    top = ly.create_cell(top_cell_name)
    for spec in box_specs:
        layer = int(spec["layer"])
        datatype = int(spec["datatype"])
        li = ly.layer(layer, datatype)
        # μm → dbu（用 round 保持精度，与 grid 检查一致）
        left_dbu = int(round(spec["left"] / dbu_um))
        bottom_dbu = int(round(spec["bottom"] / dbu_um))
        right_dbu = int(round(spec["right"] / dbu_um))
        top_dbu = int(round(spec["top"] / dbu_um))
        box = db.Box(left_dbu, bottom_dbu, right_dbu, top_dbu)
        top.shapes(li).insert(box)
    ly.write(str(output_path))
    return output_path


# =============================================================================
# 共享 fixtures
# =============================================================================
@pytest.fixture
def on_grid_polygon_gds(tmp_path: Path) -> Path:
    """创建所有顶点在 5nm grid 上的 polygon GDSII。

    矩形: (0,0)-(10,5) μm，所有坐标是 5nm 的倍数（0, 5, 10...nm）。
    grid_um=0.005 → grid_dbu=5，应无违规。
    """
    cells_spec = [
        {
            "name": "TOP",
            "polygons": [
                {
                    "layer": 1,
                    "datatype": 0,
                    "points": [[0, 0], [10, 0], [10, 5], [0, 5]],
                },
            ],
            "is_top": True,
        }
    ]
    out = tmp_path / "on_grid.gds"
    export_gdsii_from_cells(cells_spec, out)
    return out


@pytest.fixture
def off_grid_polygon_gds(tmp_path: Path) -> Path:
    """创建含 off-grid 顶点的 polygon GDSII。

    矩形顶点: (0,0), (0.007,0), (0.007,0.005),"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell_name: 顶层 cell 名。

    Returns:
        GDSII 文件路径。
    """
    import klayout.db as db

    ly = db.Layout()
    ly.dbu = dbu_um
    top = ly.create_cell(top_cell_name)
    for spec in box_specs:
        layer = int(spec["layer"])
        datatype = int(spec["datatype"])
        li = ly.layer(layer, datatype)
        # μm → dbu（用 round 保持精度，与 grid 检查一致）
        left_dbu = int(round(spec["left"] / dbu_um))
        bottom_dbu = int(round(spec["bottom"] / dbu_um))
        right_dbu = int(round(spec["right"] / dbu_um))
        top_dbu = int(round(spec["top"] / dbu_um))
        box = db.Box(left_dbu, bottom_dbu, right_dbu, top_dbu)
        top.shapes(li).insert(box)
    ly.write(str(output_path))
    return output_path


# =============================================================================
# 共享 fixtures
# =============================================================================
@pytest.fixture
def on_grid_polygon_gds(tmp_path: Path) -> Path:
    """创建所有顶点在 5nm grid 上的 polygon GDSII。

    矩形: (0,0)-(10,5) μm，所有坐标是 5nm 的倍数（0, 5, 10...nm）。
    grid_um=0.005 → grid_dbu=5，应无违规。
    """
    cells_spec = [
        {
            "name": "TOP",
            "polygons": [
                {
                    "layer": 1,
                    "datatype": 0,
                    "points": [[0, 0], [10, 0], [10, 5], [0, 5]],
                },
            ],
            "is_top": True,
        }
    ]
    out = tmp_path / "on_grid.gds"
    export_gdsii_from_cells(cells_spec, out)
    return out


@pytest.fixture
def off_grid_polygon_gds(tmp_path: Path) -> Path:
    """创建含 off-grid 顶点的 polygon GDSII。

    矩形顶点: (0,0), (0.007,0), (0.007,0.005), (0,0.005) μm
    0.007μm = 7dbu，grid_dbu=5 时 7%5=2 → off-grid
"""R325 GDSII 网格对齐检查器测试。

覆盖:
- check_grid_alignment: on-grid/off-grid 检测（polygon/box/子 cell 递归）
- generate_grid_check_report: text/markdown 报告
- GridViolation / GridCheckReport: 数据类
- R03 错误处理（文件不存在/grid_um<=0/top_cell 不存在/不支持格式）
- R02 学术诚信（docstring URL/__all__/默认值）
- 集成测试（端到端 + 与 R324 变换后检查）

来源:
- KLayout DRC grid check:
  https://klayout.org/downloads/master/doc-qt5/about/drc_ref.html
- KLayout Polygon class:
  https://www.klayout.org/doc-qt5/code/class_Polygon.html
- KLayout Box class:
  https://www.klayout.org/doc-qt5/code/class_Box.html
- KLayout Shape class:
  https://www.klayout.org/doc-qt4/code/class_Shape.html
"""

from __future__ import annotations

from pathlib import Path

import pytest

from polaris.pdk.gdsfactory_integration import export_gdsii_from_cells
from polaris.verification.gdsii_grid_alignment_checker import (
    GridCheckReport,
    GridViolation,
    check_grid_alignment,
    generate_grid_check_report,
)


# =============================================================================
# 辅助函数: 直接用 klayout API 创建含 Box 的 GDSII
# =============================================================================
def _create_gds_with_box(
    output_path: Path,
    box_specs: list[dict],
    dbu_um: float = 0.001,
    top_cell_name: str = "TOP",
) -> Path:
    """直接用 klayout API 创建含 Box shape 的 GDSII。

    create_gdsii_layout_from_cells 不支持 box 字段，故用 klayout API 直接构造。

    Args:
        output_path: 输出路径。
        box_specs: box 规格列表，每个含 layer/datatype/left/bottom/right/top
                   （单位 μm）。
        dbu_um: 数据库单位（μm）。
        top_cell_name: 顶层 cell 名。

    Returns:
        GDSII 文件路径。
    """
    import klayout.db as db

    ly = db.Layout()
    ly.dbu = dbu_um
    top = ly.create_cell(top_cell_name)
    for spec in box_specs:
        layer = int(spec["layer"])
        datatype = int(spec["datatype"])
        li = ly.layer(layer, datatype)
        # μm → dbu（用 round 保持精度，与 grid 检查一致）
        left_dbu = int(round(spec["left"] / dbu_um))
        bottom_dbu = int(round(spec["bottom"] / dbu_um))
        right_dbu = int(round(spec["right"] / dbu_um))
        top_dbu = int(round(spec["top"] / dbu_um))
        box = db.Box(left_dbu, bottom_dbu, right_dbu, top_dbu)
        top.shapes(li).insert(box)
    ly.write(str(output_path))
    return output_path


# =============================================================================
# 共享 fixtures
# =============================================================================
@pytest.fixture
def on_grid_polygon_gds(tmp_path: Path) -> Path:
    """创建所有顶点在 5nm grid 上的 polygon GDSII。

    矩形: (0,0)-(10,5) μm，所有坐标是 5nm 的倍数（0, 5, 10...nm）。
    grid_um=0.005 → grid_dbu=5，应无违规。
    """
    cells_spec = [
        {
            "name": "TOP",
            "polygons": [
                {
                    "layer": 1,
                    "datatype": 0,
                    "points": [[0, 0], [10, 0], [10, 5], [0, 5]],
                },
            ],
            "is_top": True,
        }
    ]
    out = tmp_path / "on_grid.gds"
    export_gdsii_from_cells(cells_spec, out)
    return out


@pytest.fixture
def off_grid_polygon_gds(tmp_path: Path) -> Path:
    """创建含 off-grid 顶点的 polygon GDSII。

    矩形顶点: (0,0), (0.007,0), (0.007,0.005), (0,0.005) μm
    0.007μm = 7dbu，grid_dbu=5 时 7%5=2 → off-grid
    0.005μm = 5db