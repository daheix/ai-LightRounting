# Python 字节码
__pycache__/
*.pyc
*.pyo
*.pyd

# 测试与类型检查缓存
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 打包产物
*.egg-info/
dist/
build/

# 虚拟环境
.venv/
venv/

# 3dtool 子仓库（git submodule，由 git 管理，不直接跟踪文件）
# 3dtool/ 目录是 git submodule gitlink，内容不提交主仓库

# 3dtool-appimage 解压目录（按需恢复，体积大 1.1G，不提交）
3dtool-appimage/

# polaris_wheels 离线 wheel（218M，本地维护，不提交）
polaris_wheels/

# 密钥与凭据
.env
*.key
credentials.json

# 大型版图文件（按需用 LFS）
*.gds
*.oas

# 训练检查点（体积大）
checkpoints/

# IDE 文件
.idea/
.vscode/
*.swp
*.swo

# 自动提交守护进程日志（V6）
auto_commit.log

polaris_wheels/
