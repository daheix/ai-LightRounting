[project]
name = "polaris-lvs"
version = "5.0.0"
description = "PoLaRIS LVS 网表一致性比对子模块（版图与原理图拓扑比对）"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
