#!/usr/bin#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process →#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json``#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON →#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** →#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVER#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

####!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- K#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLa#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PRO#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR,#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVER#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)),#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter /#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + D#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(d#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"]#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter()#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) ->#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["PORT_ALIGNMENT#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["PORT_ALIGNMENT"]
    )
    n_only_density_min = sum#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["PORT_ALIGNMENT"]
    )
    n_only_density_min = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["DENSITY_MIN"]
    )
    n_pa_and_density = sum(
        1 for c#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["PORT_ALIGNMENT"]
    )
    n_only_density_min = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["DENSITY_MIN"]
    )
    n_pa_and_density = sum(
        1 for c in per_circuit
        if set(c["violated_rules"]) == {"PORT_ALIGNMENT", "#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["PORT_ALIGNMENT"]
    )
    n_only_density_min = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["DENSITY_MIN"]
    )
    n_pa_and_density = sum(
        1 for c in per_circuit
        if set(c["violated_rules"]) == {"PORT_ALIGNMENT", "DENSITY_MIN"}
    )

    # 按#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["PORT_ALIGNMENT"]
    )
    n_only_density_min = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["DENSITY_MIN"]
    )
    n_pa_and_density = sum(
        1 for c in per_circuit
        if set(c["violated_rules"]) == {"PORT_ALIGNMENT", "DENSITY_MIN"}
    )

    # 按拓扑统计误报/真违规
    topo#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["PORT_ALIGNMENT"]
    )
    n_only_density_min = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["DENSITY_MIN"]
    )
    n_pa_and_density = sum(
        1 for c in per_circuit
        if set(c["violated_rules"]) == {"PORT_ALIGNMENT", "DENSITY_MIN"}
    )

    # 按拓扑统计误报/真违规
    topo_breakdown: dict[str, dict] = defaultdict(
        lambda: {"fp": 0,#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["PORT_ALIGNMENT"]
    )
    n_only_density_min = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["DENSITY_MIN"]
    )
    n_pa_and_density = sum(
        1 for c in per_circuit
        if set(c["violated_rules"]) == {"PORT_ALIGNMENT", "DENSITY_MIN"}
    )

    # 按拓扑统计误报/真违规
    topo_breakdown: dict[str, dict] = defaultdict(
        lambda: {"fp": 0, "true": 0, "total": 0}
    )
    for c in per_circuit:
        topo = c["topology"]
        topo_breakdown[topo]["total"] +=#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["PORT_ALIGNMENT"]
    )
    n_only_density_min = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["DENSITY_MIN"]
    )
    n_pa_and_density = sum(
        1 for c in per_circuit
        if set(c["violated_rules"]) == {"PORT_ALIGNMENT", "DENSITY_MIN"}
    )

    # 按拓扑统计误报/真违规
    topo_breakdown: dict[str, dict] = defaultdict(
        lambda: {"fp": 0, "true": 0, "total": 0}
    )
    for c in per_circuit:
        topo = c["topology"]
        topo_breakdown[topo]["total"] += 1
        if c["is_false_positive#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["PORT_ALIGNMENT"]
    )
    n_only_density_min = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["DENSITY_MIN"]
    )
    n_pa_and_density = sum(
        1 for c in per_circuit
        if set(c["violated_rules"]) == {"PORT_ALIGNMENT", "DENSITY_MIN"}
    )

    # 按拓扑统计误报/真违规
    topo_breakdown: dict[str, dict] = defaultdict(
        lambda: {"fp": 0, "true": 0, "total": 0}
    )
    for c in per_circuit:
        topo = c["topology"]
        topo_breakdown[topo]["total"] += 1
        if c["is_false_positive"]:
            topo_breakdown[topo]["fp"]#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["PORT_ALIGNMENT"]
    )
    n_only_density_min = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["DENSITY_MIN"]
    )
    n_pa_and_density = sum(
        1 for c in per_circuit
        if set(c["violated_rules"]) == {"PORT_ALIGNMENT", "DENSITY_MIN"}
    )

    # 按拓扑统计误报/真违规
    topo_breakdown: dict[str, dict] = defaultdict(
        lambda: {"fp": 0, "true": 0, "total": 0}
    )
    for c in per_circuit:
        topo = c["topology"]
        topo_breakdown[topo]["total"] += 1
        if c["is_false_positive"]:
            topo_breakdown[topo]["fp"] += 1
        elif c["is_true_violation"]:
            topo_breakdown[topo]["true"] += 1

    # 规则分布表格
    rule_rows = sorted#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["PORT_ALIGNMENT"]
    )
    n_only_density_min = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["DENSITY_MIN"]
    )
    n_pa_and_density = sum(
        1 for c in per_circuit
        if set(c["violated_rules"]) == {"PORT_ALIGNMENT", "DENSITY_MIN"}
    )

    # 按拓扑统计误报/真违规
    topo_breakdown: dict[str, dict] = defaultdict(
        lambda: {"fp": 0, "true": 0, "total": 0}
    )
    for c in per_circuit:
        topo = c["topology"]
        topo_breakdown[topo]["total"] += 1
        if c["is_false_positive"]:
            topo_breakdown[topo]["fp"] += 1
        elif c["is_true_violation"]:
            topo_breakdown[topo]["true"] += 1

    # 规则分布表格
    rule_rows = sorted(rule_counter.items(), key=lambda x: -x[1])

    lines: list[str] =#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["PORT_ALIGNMENT"]
    )
    n_only_density_min = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["DENSITY_MIN"]
    )
    n_pa_and_density = sum(
        1 for c in per_circuit
        if set(c["violated_rules"]) == {"PORT_ALIGNMENT", "DENSITY_MIN"}
    )

    # 按拓扑统计误报/真违规
    topo_breakdown: dict[str, dict] = defaultdict(
        lambda: {"fp": 0, "true": 0, "total": 0}
    )
    for c in per_circuit:
        topo = c["topology"]
        topo_breakdown[topo]["total"] += 1
        if c["is_false_positive"]:
            topo_breakdown[topo]["fp"] += 1
        elif c["is_true_violation"]:
            topo_breakdown[topo]["true"] += 1

    # 规则分布表格
    rule_rows = sorted(rule_counter.items(), key=lambda x: -x[1])

    lines: list[str] = []
    lines.append("# DRC 误报#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["PORT_ALIGNMENT"]
    )
    n_only_density_min = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["DENSITY_MIN"]
    )
    n_pa_and_density = sum(
        1 for c in per_circuit
        if set(c["violated_rules"]) == {"PORT_ALIGNMENT", "DENSITY_MIN"}
    )

    # 按拓扑统计误报/真违规
    topo_breakdown: dict[str, dict] = defaultdict(
        lambda: {"fp": 0, "true": 0, "total": 0}
    )
    for c in per_circuit:
        topo = c["topology"]
        topo_breakdown[topo]["total"] += 1
        if c["is_false_positive"]:
            topo_breakdown[topo]["fp"] += 1
        elif c["is_true_violation"]:
            topo_breakdown[topo]["true"] += 1

    # 规则分布表格
    rule_rows = sorted(rule_counter.items(), key=lambda x: -x[1])

    lines: list[str] = []
    lines.append("# DRC 误报全量审计报告（PoLaRIS Task 1）")
    lines.append("")
    lines.append(
        f"**生成时间**: {#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["PORT_ALIGNMENT"]
    )
    n_only_density_min = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["DENSITY_MIN"]
    )
    n_pa_and_density = sum(
        1 for c in per_circuit
        if set(c["violated_rules"]) == {"PORT_ALIGNMENT", "DENSITY_MIN"}
    )

    # 按拓扑统计误报/真违规
    topo_breakdown: dict[str, dict] = defaultdict(
        lambda: {"fp": 0, "true": 0, "total": 0}
    )
    for c in per_circuit:
        topo = c["topology"]
        topo_breakdown[topo]["total"] += 1
        if c["is_false_positive"]:
            topo_breakdown[topo]["fp"] += 1
        elif c["is_true_violation"]:
            topo_breakdown[topo]["true"] += 1

    # 规则分布表格
    rule_rows = sorted(rule_counter.items(), key=lambda x: -x[1])

    lines: list[str] = []
    lines.append("# DRC 误报全量审计报告（PoLaRIS Task 1）")
    lines.append("")
    lines.append(
        f"**生成时间**: {time.strftime('%Y-%m-%d %H:%M:%S CST', time.localtime())}"
    )
    lines.append(
        f"**#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["PORT_ALIGNMENT"]
    )
    n_only_density_min = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["DENSITY_MIN"]
    )
    n_pa_and_density = sum(
        1 for c in per_circuit
        if set(c["violated_rules"]) == {"PORT_ALIGNMENT", "DENSITY_MIN"}
    )

    # 按拓扑统计误报/真违规
    topo_breakdown: dict[str, dict] = defaultdict(
        lambda: {"fp": 0, "true": 0, "total": 0}
    )
    for c in per_circuit:
        topo = c["topology"]
        topo_breakdown[topo]["total"] += 1
        if c["is_false_positive"]:
            topo_breakdown[topo]["fp"] += 1
        elif c["is_true_violation"]:
            topo_breakdown[topo]["true"] += 1

    # 规则分布表格
    rule_rows = sorted(rule_counter.items(), key=lambda x: -x[1])

    lines: list[str] = []
    lines.append("# DRC 误报全量审计报告（PoLaRIS Task 1）")
    lines.append("")
    lines.append(
        f"**生成时间**: {time.strftime('%Y-%m-%d %H:%M:%S CST', time.localtime())}"
    )
    lines.append(
        f"**审计脚本**: `/workspace/scripts/audit_drc#!/usr/bin/env python3
"""DRC 误报全量审计脚本（PoLaRIS Task 1）。

读取上一轮 1200 电路端到端测试结果（progress.json），对所有 DRC 失败电路
重新跑 DRC 收集详细违规，按规则名统计违规分布，抽样 50 个 PORT_ALIGNMENT
违规电路分析 dx/dy 偏差分布，判断是布局算法局限（误报）还是真违规，
输出误报率与修复建议报告。

## Input → Process → Output 三段式

### Input
- ``/workspace/out/batch_test/progress.json`` — 1200 电路端到端测试结果
- ``/workspace/data/benchmarks/generated/`` — 1200 个电路 JSON + index.json

### Process
1. 读取 progress.json，找出所有 ``drc_passed=False`` 的电路
2. 对每个 DRC 失败电路：加载 circuit JSON → ``place_circuit(mode="analytical")``
   → ``run_drc`` → 收集 violations
3. 按规则名（PORT_ALIGNMENT / PORT_FACING / DENSITY_MAX 等）统计违规分布
4. 抽样 50 个 PORT_ALIGNMENT 违规电路，解析 dx/dy 偏差，判断误报
5. 误报判定：
   - **PORT_ALIGNMENT 违规** → 误报（布局算法局限，端口未对齐但电路结构合法，
     器件不重叠/间距满足/方向相对，可通过波导弯曲补偿）
   - **DENSITY_MIN 违规** → 误报（benchmark 画布尺寸与器件规模不匹配，
     电路结构本身合法，非布局算法问题）
   - **其他规则违规**（MIN_SPACING / NO_OVERLAP / BOUNDARY / PORT_DIRECTION /
     PORT_FACING / PORT_CONNECTIVITY / DENSITY_MAX / MIN_WIDTH / MIN_HEIGHT /
     MIN_AREA）→ 真违规（器件真实重叠/间距不足/方向非法/未连接）

### Output
- ``/workspace/out/audit/drc_false_positive_report.md`` — 误报分析报告
- ``/workspace/out/audit/drc_audit_data.json`` — 完整审计数据（中间产物）

## 误报判定依据（R02 学术诚信）

### 真违规定义
器件真实重叠（NO_OVERLAP）/ 间距不足（MIN_SPACING）/ 方向非法
（PORT_DIRECTION / PORT_FACING）/ 未连接（PORT_CONNECTIVITY）/ 超出边界
（BOUNDARY）/ 密度超限（DENSITY_MAX）/ 尺寸不足（MIN_WIDTH / MIN_HEIGHT /
MIN_AREA）。这些违规无法通过布线补偿，必须修复布局或电路结构。

### 误报定义
布局算法局限导致端口未对齐（PORT_ALIGNMENT），但电路本身结构合法
（器件不重叠、间距满足、方向相对、连接完整）。未对齐可通过波导弯曲补偿
（每增加一个弯曲 ≈ 0.05dB 损耗，Chrostowski & Hochberg 2015 §4.3），
非工艺致命违规。PORT_ALIGNMENT 规则 severity=0.5（建议性），低于真违规
规则的 severity（0.7-1.0）。

DENSITY_MIN 违规本质是 benchmark 画布尺寸（如 XL=3000×3000μm²）与器件
规模（4 个小器件总面积 ~540μm²）不匹配，电路结构本身合法，归类为
benchmark 设计问题导致的误报。

## 来源（R02 学术诚信，≥5 个文献 URL）
- SiEPIC EBeam PDK DRC runset https://github.com/SiEPIC/SiEPIC_EBeam_PDK
- Chrostowski & Hochberg, "Silicon Photonics Design", CUP 2015 §4.3
  https://www.cambridge.org/core/books/silicon-photonics-design/
- DREAMPlace TCAD 2020 https://arxiv.org/abs/2004.10746
- KLayout DRC 文档 https://www.klayout.org/doc-qt5/manual/drc_runsets.html
- FFDH: Coffman et al. SIAM J. Comput. 9(4) 1980
  https://epubs.siam.org/doi/10.1137/0209062
- PoLaRIS DRC 引擎: /workspace/modules/drc/src/polaris_drc/engine.py
- PoLaRIS 布局器: /workspace/modules/place/src/polaris_place/analytical.py
"""

from __future__ import annotations

import json
import math
import os
import re
import sys
import time
from collections import Counter, defaultdict
from typing import Any

# 添加 PoLaRIS 子模块路径（editable install 也可，此处双保险）
sys.path.insert(0, "/workspace/modules/drc/src")
sys.path.insert(0, "/workspace/modules/place/src")

import polaris_drc  # noqa: E402
import polaris_place  # noqa: E402

# =========================================================================
# 路径与常量
# =========================================================================
PROGRESS_PATH = "/workspace/out/batch_test/progress.json"
CIRCUITS_DIR = "/workspace/data/benchmarks/generated"
INDEX_PATH = os.path.join(CIRCUITS_DIR, "index.json")
OUTPUT_DIR = "/workspace/out/audit"
REPORT_PATH = os.path.join(OUTPUT_DIR, "drc_false_positive_report.md")
DATA_PATH = os.path.join(OUTPUT_DIR, "drc_audit_data.json")

# PORT_ALIGNMENT 容差（与 polaris_drc/engine.py _PORT_ALIGN_TOL_UM 一致）
PORT_ALIGN_TOL_UM = 5.0
# 抽样数量
PORT_ALIGN_SAMPLE_SIZE = 50
# 误报判定阈值：PORT_ALIGNMENT 偏差在此范围内视为布局算法局限（误报）
# dx/dy 都 < 50μm 视为布局算法装箱偏差（可通过波导弯曲补偿）
PORT_ALIGN_FP_THRESHOLD_UM = 50.0

# 真违规规则集合（出现任一即为真违规，非误报）
_TRUE_VIOLATION_RULES = frozenset({
    "MIN_SPACING", "MIN_WIDTH", "MIN_HEIGHT", "MIN_AREA",
    "BOUNDARY", "NO_OVERLAP",
    "PORT_DIRECTION", "PORT_CONNECTIVITY", "PORT_FACING",
    "DENSITY_MAX",
})


# =========================================================================
# 工具函数
# =========================================================================
def load_json(path: str) -> Any:
    """加载 JSON 文件（R03: 失败 raise）。"""
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def find_circuit_path(name: str, index: dict) -> str:
    """根据电路名从 index.json 查找 circuit JSON 路径。

    R03: 未找到 raise，不返回 None。
    """
    for entry in index.get("circuits", []):
        if entry.get("name") == name:
            return os.path.join(CIRCUITS_DIR, entry["path"])
    raise RuntimeError(
        f"电路 {name} 在 index.json 中未找到（R03 禁止 fall-back）"
    )


def parse_dx_dy_from_message(msg: str) -> tuple[float, float]:
    """从 PORT_ALIGNMENT 违规 message 中解析 dx/dy。

    message 格式: "PORT_ALIGNMENT: 连接 d1.p1→d2.p2 端口未对齐
                   dx=12.34μm dy=56.78μm > 容差 5.00μm"

    R03: 解析失败 raise。
    """
    m = re.search(r"dx=([\d.]+)μm\s+dy=([\d.]+)μm", msg)
    if not m:
        raise RuntimeError(
            f"无法从 PORT_ALIGNMENT message 解析 dx/dy: {msg!r}"
            f"（R03 禁止 fall-back）"
        )
    return float(m.group(1)), float(m.group(2))


def parse_density_from_message(msg: str) -> float:
    """从 DENSITY_MIN/DENSITY_MAX 违规 message 中解析密度百分比。

    message 格式: "DENSITY_MIN: 布局密度 0.0060% 低于下限 0.0100%"

    R03: 解析失败 raise。
    """
    m = re.search(r"布局密度\s+([\d.]+)%", msg)
    if not m:
        raise RuntimeError(
            f"无法从 DENSITY message 解析密度: {msg!r}（R03 禁止 fall-back）"
        )
    return float(m.group(1))


# =========================================================================
# 主审计流程
# =========================================================================
def audit_all_failures(progress: dict, index: dict) -> dict:
    """对所有 DRC 失败电路重跑 DRC，收集详细违规。

    Args:
        progress: progress.json 数据。
        index: index.json 数据。

    Returns:
        审计数据 dict，含 rule_counter / rule_by_topology /
        port_align_samples / per_circuit 详情。
    """
    results = progress.get("results", [])
    total = len(results)
    drc_fail = [
        r for r in results
        if r.get("success") and r.get("drc_passed") is False
    ]
    drc_pass = [r for r in results if r.get("success") and r.get("drc_passed")]
    run_fail = [r for r in results if not r.get("success")]

    print(f"[audit] 总电路: {total}")
    print(f"[audit] 运行成功 + DRC 通过: {len(drc_pass)}")
    print(f"[audit] 运行成功 + DRC 失败: {len(drc_fail)}（待审计）")
    print(f"[audit] 运行失败: {len(run_fail)}（不在本审计范围）")
    print(f"[audit] 开始逐个重跑 DRC ...")

    rule_counter: Counter = Counter()
    rule_by_topology: dict[str, Counter] = defaultdict(Counter)
    topology_counter: Counter = Counter()
    port_align_samples: list[dict] = []
    per_circuit: list[dict] = []
    audit_start = time.perf_counter()

    for i, r in enumerate(drc_fail):
        name = r["name"]
        topology = r.get("topology", "unknown")
        circuit_path = find_circuit_path(name, index)
        circuit = load_json(circuit_path)

        # 重新执行布局 + DRC（与 orchestrator flow stage 3+6 一致）
        place_result = polaris_place.place_circuit(circuit, mode="analytical")
        placements = place_result["placements"]
        drc_result = polaris_drc.run_drc(circuit, placements)
        violations = drc_result["violations"]
        violated_rules = {v["rule_name"] for v in violations}

        for v in violations:
            rule_counter[v["rule_name"]] += 1
            rule_by_topology[topology][v["rule_name"]] += 1
            if v["rule_name"] == "PORT_ALIGNMENT":
                dx, dy = parse_dx_dy_from_message(v["message"])
                port_align_samples.append({
                    "circuit": name,
                    "topology": topology,
                    "connection": v["device_name"],
                    "dx": dx,
                    "dy": dy,
                    "dist_um": math.hypot(dx, dy),
                    "message": v["message"],
                })

        # 判定是否误报：仅含 PORT_ALIGNMENT / DENSITY_MIN 视为误报
        is_false_positive = violated_rules.issubset({"PORT_ALIGNMENT", "DENSITY_MIN"})
        is_true_violation = bool(violated_rules & _TRUE_VIOLATION_RULES)
        # 密度信息（如有）
        density_pct = None
        for v in violations:
            if v["rule_name"] in ("DENSITY_MIN", "DENSITY_MAX"):
                density_pct = parse_density_from_message(v["message"])
                break

        per_circuit.append({
            "name": name,
            "topology": topology,
            "scale": r.get("scale", ""),
            "platform": r.get("platform", ""),
            "n_devices": r.get("n_devices", 0),
            "n_violations": len(violations),
            "violated_rules": sorted(violated_rules),
            "is_false_positive": is_false_positive,
            "is_true_violation": is_true_violation,
            "density_pct": density_pct,
            "canvas_w": circuit.get("canvas_w"),
            "canvas_h": circuit.get("canvas_h"),
        })
        topology_counter[topology] += 1

        if (i + 1) % 50 == 0 or (i + 1) == len(drc_fail):
            elapsed = time.perf_counter() - audit_start
            print(
                f"[audit] [{i + 1}/{len(drc_fail)}] "
                f"已处理 {i + 1} 个失败电路，耗时 {elapsed:.1f}s"
            )

    elapsed_total = time.perf_counter() - audit_start
    print(f"[audit] 全量审计完成，总耗时 {elapsed_total:.1f}s")

    return {
        "total_circuits": total,
        "n_drc_passed": len(drc_pass),
        "n_drc_failed": len(drc_fail),
        "n_run_failed": len(run_fail),
        "rule_counter": dict(rule_counter),
        "rule_by_topology": {k: dict(v) for k, v in rule_by_topology.items()},
        "topology_counter": dict(topology_counter),
        "port_align_samples": port_align_samples,
        "per_circuit": per_circuit,
        "audit_duration_sec": elapsed_total,
    }


def sample_port_alignment(samples: list[dict],
                          target_size: int) -> list[dict]:
    """从 PORT_ALIGNMENT 违规样本中均匀抽样，覆盖不同拓扑。

    轮询每个拓扑取一个样本，直到达到 target_size 或样本耗尽。

    Args:
        samples: 全部 PORT_ALIGNMENT 违规样本。
        target_size: 目标抽样数（50）。

    Returns:
        抽样后的样本列表。
    """
    if len(samples) <= target_size:
        return list(samples)
    by_topo: dict[str, list[dict]] = defaultdict(list)
    for s in samples:
        by_topo[s["topology"]].append(s)
    topo_keys = list(by_topo.keys())
    idx_in_topo = {k: 0 for k in topo_keys}
    sampled: list[dict] = []
    while len(sampled) < target_size:
        progressed = False
        for k in topo_keys:
            if idx_in_topo[k] < len(by_topo[k]):
                sampled.append(by_topo[k][idx_in_topo[k]])
                idx_in_topo[k] += 1
                progressed = True
                if len(sampled) >= target_size:
                    break
        if not progressed:
            break
    return sampled


def compute_statistics(samples: list[dict]) -> dict:
    """计算 PORT_ALIGNMENT 抽样的 dx/dy/dist 统计。"""
    if not samples:
        return {
            "n": 0, "dx_min": 0, "dx_max": 0, "dx_mean": 0, "dx_median": 0,
            "dy_min": 0, "dy_max": 0, "dy_mean": 0, "dy_median": 0,
            "dist_min": 0, "dist_max": 0, "dist_mean": 0, "dist_median": 0,
            "n_fp_layout_limit": 0, "n_severe": 0,
        }
    dxs = sorted(s["dx"] for s in samples)
    dys = sorted(s["dy"] for s in samples)
    dists = sorted(s["dist_um"] for s in samples)
    n = len(samples)
    # 布局算法局限误报：dx 和 dy 都 < 50μm（装箱偏差，可波导弯曲补偿）
    n_fp_layout = sum(
        1 for s in samples
        if s["dx"] < PORT_ALIGN_FP_THRESHOLD_UM
        and s["dy"] < PORT_ALIGN_FP_THRESHOLD_UM
    )
    # 严重偏差：dx 或 dy >= 50μm（布局算法严重失败，需重点关注）
    n_severe = n - n_fp_layout

    def median(arr: list[float]) -> float:
        mid = len(arr) // 2
        if len(arr) % 2 == 0:
            return (arr[mid - 1] + arr[mid]) / 2.0
        return arr[mid]

    return {
        "n": n,
        "dx_min": dxs[0], "dx_max": dxs[-1],
        "dx_mean": sum(dxs) / n, "dx_median": median(dxs),
        "dy_min": dys[0], "dy_max": dys[-1],
        "dy_mean": sum(dys) / n, "dy_median": median(dys),
        "dist_min": dists[0], "dist_max": dists[-1],
        "dist_mean": sum(dists) / n, "dist_median": median(dists),
        "n_fp_layout_limit": n_fp_layout,
        "n_severe": n_severe,
    }


# =========================================================================
# 报告生成
# =========================================================================
def generate_report(audit_data: dict, sample: list[dict],
                    stats: dict) -> str:
    """生成 Markdown 误报分析报告。"""
    total = audit_data["total_circuits"]
    n_pass = audit_data["n_drc_passed"]
    n_fail = audit_data["n_drc_failed"]
    n_run_fail = audit_data["n_run_failed"]
    rule_counter = audit_data["rule_counter"]
    rule_by_topology = audit_data["rule_by_topology"]
    topology_counter = audit_data["topology_counter"]
    per_circuit = audit_data["per_circuit"]
    audit_dur = audit_data["audit_duration_sec"]
    all_pa_samples = audit_data["port_align_samples"]

    # 误报统计
    n_fp = sum(1 for c in per_circuit if c["is_false_positive"])
    n_true = sum(1 for c in per_circuit if c["is_true_violation"])
    n_mixed = n_fail - n_fp - n_true
    # 误报率 = 误报电路数 / DRC 失败电路数
    fp_rate = n_fp / n_fail if n_fail > 0 else 0.0

    # 仅含 PORT_ALIGNMENT 的电路数
    n_only_pa = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["PORT_ALIGNMENT"]
    )
    n_only_density_min = sum(
        1 for c in per_circuit
        if c["violated_rules"] == ["DENSITY_MIN"]
    )
    n_pa_and_density = sum(
        1 for c in per_circuit
        if set(c["violated_rules"]) == {"PORT_ALIGNMENT", "DENSITY_MIN"}
    )

    # 按拓扑统计误报/真违规
    topo_breakdown: dict[str, dict] = defaultdict(
        lambda: {"fp": 0, "true": 0, "total": 0}
    )
    for c in per_circuit:
        topo = c["topology"]
        topo_breakdown[topo]["total"] += 1
        if c["is_false_positive"]:
            topo_breakdown[topo]["fp"] += 1
        elif c["is_true_violation"]:
            topo_breakdown[topo]["true"] += 1

    # 规则分布表格
    rule_rows = sorted(rule_counter.items(), key=lambda x: -x[1])

    lines: list[str] = []
    lines.append("# DRC 误报全量审计报告（PoLaRIS Task 1）")
    lines.append("")
    lines.append(
        f"**生成时间**: {time.strftime('%Y-%m-%d %H:%M:%S CST', time.localtime())}"
    )
    lines.append(
        f"**审计脚本**: `/workspace/scripts/audit_drc_false_positives.py`"
    )
    lines.append(
        f"**数据来源**: `/workspace/out/batch_test/progress.json`（