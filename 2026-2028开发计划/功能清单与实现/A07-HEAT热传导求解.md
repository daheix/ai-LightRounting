# A07 - HEAT 热# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 |# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOpt# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 Max# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
|# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

### A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\math# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} =# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-N# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\math# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}]# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

#### A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 AD# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4.# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COM# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dir# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数。辐射项非线性，需 Newton 迭代线性化。
5. **Contact（接触热阻）**：$-k \partial T# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数。辐射项非线性，需 Newton 迭代线性化。
5. **Contact（接触热阻）**：$-k \partial T/\partial n|_{\Gamma} = (T - T_{contact})/R_{th}$，$R_{th}$ 为界面热阻（m²·K/W）。

### A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数。辐射项非线性，需 Newton 迭代线性化。
5. **Contact（接触热阻）**：$-k \partial T/\partial n|_{\Gamma} = (T - T_{contact})/R_{th}$，$R_{th}$ 为界面热阻（m²·K/W）。

## 5. 核心算法逻辑（完整# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数。辐射项非线性，需 Newton 迭代线性化。
5. **Contact（接触热阻）**：$-k \partial T/\partial n|_{\Gamma} = (T - T_{contact})/R_{th}$，$R_{th}$ 为界面热阻（m²·K/W）。

## 5. 核心算法逻辑（完整伪代码）

```
ALGORITHM HEAT_Solve(mesh, materials, sources, bcs,# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数。辐射项非线性，需 Newton 迭代线性化。
5. **Contact（接触热阻）**：$-k \partial T/\partial n|_{\Gamma} = (T - T_{contact})/R_{th}$，$R_{th}$ 为界面热阻（m²·K/W）。

## 5. 核心算法逻辑（完整伪代码）

```
ALGORITHM HEAT_Solve(mesh, materials, sources, bcs, mode, dt, t_end):
  # 输入：
  #   mesh     =# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数。辐射项非线性，需 Newton 迭代线性化。
5. **Contact（接触热阻）**：$-k \partial T/\partial n|_{\Gamma} = (T - T_{contact})/R_{th}$，$R_{th}$ 为界面热阻（m²·K/W）。

## 5. 核心算法逻辑（完整伪代码）

```
ALGORITHM HEAT_Solve(mesh, materials, sources, bcs, mode, dt, t_end):
  # 输入：
  #   mesh     = 节点坐标 + 单元连接表 +# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数。辐射项非线性，需 Newton 迭代线性化。
5. **Contact（接触热阻）**：$-k \partial T/\partial n|_{\Gamma} = (T - T_{contact})/R_{th}$，$R_{th}$ 为界面热阻（m²·K/W）。

## 5. 核心算法逻辑（完整伪代码）

```
ALGORITHM HEAT_Solve(mesh, materials, sources, bcs, mode, dt, t_end):
  # 输入：
  #   mesh     = 节点坐标 + 单元连接表 + 控制体积 ΔV_i
  ## A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数。辐射项非线性，需 Newton 迭代线性化。
5. **Contact（接触热阻）**：$-k \partial T/\partial n|_{\Gamma} = (T - T_{contact})/R_{th}$，$R_{th}$ 为界面热阻（m²·K/W）。

## 5. 核心算法逻辑（完整伪代码）

```
ALGORITHM HEAT_Solve(mesh, materials, sources, bcs, mode, dt, t_end):
  # 输入：
  #   mesh     = 节点坐标 + 单元连接表 + 控制体积 ΔV_i
  #   materials= 每区域 (ρ, c_p# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数。辐射项非线性，需 Newton 迭代线性化。
5. **Contact（接触热阻）**：$-k \partial T/\partial n|_{\Gamma} = (T - T_{contact})/R_{th}$，$R_{th}$ 为界面热阻（m²·K/W）。

## 5. 核心算法逻辑（完整伪代码）

```
ALGORITHM HEAT_Solve(mesh, materials, sources, bcs, mode, dt, t_end):
  # 输入：
  #   mesh     = 节点坐标 + 单元连接表 + 控制体积 ΔV_i
  #   materials= 每区域 (ρ, c_p, k(T))，k 可温度依赖
# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数。辐射项非线性，需 Newton 迭代线性化。
5. **Contact（接触热阻）**：$-k \partial T/\partial n|_{\Gamma} = (T - T_{contact})/R_{th}$，$R_{th}$ 为界面热阻（m²·K/W）。

## 5. 核心算法逻辑（完整伪代码）

```
ALGORITHM HEAT_Solve(mesh, materials, sources, bcs, mode, dt, t_end):
  # 输入：
  #   mesh     = 节点坐标 + 单元连接表 + 控制体积 ΔV_i
  #   materials= 每区域 (ρ, c_p, k(T))，k 可温度依赖
  #   sources  = 热源列表 [(region, Q_type, value)]
  ## A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数。辐射项非线性，需 Newton 迭代线性化。
5. **Contact（接触热阻）**：$-k \partial T/\partial n|_{\Gamma} = (T - T_{contact})/R_{th}$，$R_{th}$ 为界面热阻（m²·K/W）。

## 5. 核心算法逻辑（完整伪代码）

```
ALGORITHM HEAT_Solve(mesh, materials, sources, bcs, mode, dt, t_end):
  # 输入：
  #   mesh     = 节点坐标 + 单元连接表 + 控制体积 ΔV_i
  #   materials= 每区域 (ρ, c_p, k(T))，k 可温度依赖
  #   sources  = 热源列表 [(region, Q_type, value)]
  #              Q_type ∈ {VOLUME, JOULE, OPTICAL_ABSORPTION}
  #   bcs      = 边界条件列表 [(boundary, type, params)]
  #              type# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数。辐射项非线性，需 Newton 迭代线性化。
5. **Contact（接触热阻）**：$-k \partial T/\partial n|_{\Gamma} = (T - T_{contact})/R_{th}$，$R_{th}$ 为界面热阻（m²·K/W）。

## 5. 核心算法逻辑（完整伪代码）

```
ALGORITHM HEAT_Solve(mesh, materials, sources, bcs, mode, dt, t_end):
  # 输入：
  #   mesh     = 节点坐标 + 单元连接表 + 控制体积 ΔV_i
  #   materials= 每区域 (ρ, c_p, k(T))，k 可温度依赖
  #   sources  = 热源列表 [(region, Q_type, value)]
  #              Q_type ∈ {VOLUME, JOULE, OPTICAL_ABSORPTION}
  #   bcs      = 边界条件列表 [(boundary, type, params)]
  #              type ∈ {DIRICHLET, NEUMANN, ROBIN, RADIATION, CONTACT}
# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数。辐射项非线性，需 Newton 迭代线性化。
5. **Contact（接触热阻）**：$-k \partial T/\partial n|_{\Gamma} = (T - T_{contact})/R_{th}$，$R_{th}$ 为界面热阻（m²·K/W）。

## 5. 核心算法逻辑（完整伪代码）

```
ALGORITHM HEAT_Solve(mesh, materials, sources, bcs, mode, dt, t_end):
  # 输入：
  #   mesh     = 节点坐标 + 单元连接表 + 控制体积 ΔV_i
  #   materials= 每区域 (ρ, c_p, k(T))，k 可温度依赖
  #   sources  = 热源列表 [(region, Q_type, value)]
  #              Q_type ∈ {VOLUME, JOULE, OPTICAL_ABSORPTION}
  #   bcs      = 边界条件列表 [(boundary, type, params)]
  #              type ∈ {DIRICHLET, NEUMANN, ROBIN, RADIATION, CONTACT}
  #   mode     = STEADY / TRANS# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数。辐射项非线性，需 Newton 迭代线性化。
5. **Contact（接触热阻）**：$-k \partial T/\partial n|_{\Gamma} = (T - T_{contact})/R_{th}$，$R_{th}$ 为界面热阻（m²·K/W）。

## 5. 核心算法逻辑（完整伪代码）

```
ALGORITHM HEAT_Solve(mesh, materials, sources, bcs, mode, dt, t_end):
  # 输入：
  #   mesh     = 节点坐标 + 单元连接表 + 控制体积 ΔV_i
  #   materials= 每区域 (ρ, c_p, k(T))，k 可温度依赖
  #   sources  = 热源列表 [(region, Q_type, value)]
  #              Q_type ∈ {VOLUME, JOULE, OPTICAL_ABSORPTION}
  #   bcs      = 边界条件列表 [(boundary, type, params)]
  #              type ∈ {DIRICHLET, NEUMANN, ROBIN, RADIATION, CONTACT}
  #   mode     = STEADY / TRANSIENT
  #   dt, t_end= 时间步长与终止时间（仅 TRANSIENT# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数。辐射项非线性，需 Newton 迭代线性化。
5. **Contact（接触热阻）**：$-k \partial T/\partial n|_{\Gamma} = (T - T_{contact})/R_{th}$，$R_{th}$ 为界面热阻（m²·K/W）。

## 5. 核心算法逻辑（完整伪代码）

```
ALGORITHM HEAT_Solve(mesh, materials, sources, bcs, mode, dt, t_end):
  # 输入：
  #   mesh     = 节点坐标 + 单元连接表 + 控制体积 ΔV_i
  #   materials= 每区域 (ρ, c_p, k(T))，k 可温度依赖
  #   sources  = 热源列表 [(region, Q_type, value)]
  #              Q_type ∈ {VOLUME, JOULE, OPTICAL_ABSORPTION}
  #   bcs      = 边界条件列表 [(boundary, type, params)]
  #              type ∈ {DIRICHLET, NEUMANN, ROBIN, RADIATION, CONTACT}
  #   mode     = STEADY / TRANSIENT
  #   dt, t_end= 时间步长与终止时间（仅 TRANSIENT）
  # 输出：
  ## A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数。辐射项非线性，需 Newton 迭代线性化。
5. **Contact（接触热阻）**：$-k \partial T/\partial n|_{\Gamma} = (T - T_{contact})/R_{th}$，$R_{th}$ 为界面热阻（m²·K/W）。

## 5. 核心算法逻辑（完整伪代码）

```
ALGORITHM HEAT_Solve(mesh, materials, sources, bcs, mode, dt, t_end):
  # 输入：
  #   mesh     = 节点坐标 + 单元连接表 + 控制体积 ΔV_i
  #   materials= 每区域 (ρ, c_p, k(T))，k 可温度依赖
  #   sources  = 热源列表 [(region, Q_type, value)]
  #              Q_type ∈ {VOLUME, JOULE, OPTICAL_ABSORPTION}
  #   bcs      = 边界条件列表 [(boundary, type, params)]
  #              type ∈ {DIRICHLET, NEUMANN, ROBIN, RADIATION, CONTACT}
  #   mode     = STEADY / TRANSIENT
  #   dt, t_end= 时间步长与终止时间（仅 TRANSIENT）
  # 输出：
  #   T[N] 节点温度场，q[N] 节点热流密度

  # === 步骤 1：网格生成# A07 - HEAT 热传导求解（Heat Transport Solver）

> 聚类ID: A07
> 类别: 求解器类
> 优先级: P1
> 生成时间: 2026-06-25
> 关联文档: `3dtool/ALGORITHMS.md` §4、`docs/feature_gap_full_analysis.md` §6（T15-6.x）、`00-算法聚类清单.md` A07
> 学术诚信：所有公式经 Fourier 导热定律、COMSOL Heat Transfer Module 文档、Ansys Lumerical HEAT 文档与 SfePy/Pyrit 开源 FEM 实现交叉验证（规则 18），无 fall-back 编造（规则 14），纯 CPU 算法（规则 26）。

## 覆盖功能点清单

本聚类覆盖 10 个功能点，源自 `docs/feature_gap_full_analysis.md` §6 HEAT 求解器（T15 曼光 MaxOptics 6.1-6.10，对应 T01 Ansys Lumerical HEAT 同类项）。

| 编号 | 工具 | 功能点 | PoLaRIS 状态 |
|------|------|--------|------------|
| T15-6.1 | 曼光 MaxOptics | 复杂 2D/3D 结构构建与网格划分 | ❌ 缺失 |
| T15-6.2 | 曼光 MaxOptics | 瞬态与稳态热传输仿真 | ❌ 缺失 |
| T15-6.3 | 曼光 MaxOptics | 傅里叶导热方程求解（ρc_p∂T/∂t-∇·(k∇T)=q） | ❌ 缺失 |
| T15-6.4 | 曼光 MaxOptics | 多种热边界条件（5 类：温度/热流/对流/辐射/热阻） | ❌ 缺失 |
| T15-6.5 | 曼光 MaxOptics | 灵活的参数扫描工具 | ⚠️ 部分（`src/polaris/data/variant_generator.py:478` 通用扫描，非热专用） |
| T15-6.6 | 曼光 MaxOptics | 独立求解器运行 | ❌ 缺失 |
| T15-6.7 | 曼光 MaxOptics | 光-热耦合（光吸收生热+热光效应） | ❌ 缺失 |
| T15-6.8 | 曼光 MaxOptics | 电-热耦合（焦耳热+热电效应） | ❌ 缺失 |
| T15-6.9 | 曼光 MaxOptics | 多种热源支持（体积发热/焦耳热/光吸收自热） | ❌ 缺失 |
| T15-6.10 | 曼光 MaxOptics | 辐射散热边界支持（斯特藩-玻尔兹曼常数） | ❌ 缺失 |

**统计**：✅ 0 / ⚠️ 1 / ❌ 9（与聚类清单 0/3/7 基本一致，本表细化到 10 项）。

## 1. 物理模型与适用范围

HEAT 求解傅里叶导热方程，模拟光电子器件中焦耳热、光吸收热、边界对流/辐射等引起的稳态/瞬态温度分布。光电子器件（热光调相器、SiN 加热臂、激光器自热）的性能强依赖温度场，需耦合电磁/电学求解得到损耗分布作为热源，再求解温度场反作用于折射率（$dn/dT$）。

**适用范围**：
- SOI 热光马赫-曾德调相器（MZM）、SiN 微环加热臂
- 激光器自热效应（DBR/DFB/VCSEL）
- 光电探测器热噪声分析
- 海缆/光子集成封装热管理

**不适用**：纳米尺度非傅里叶导热（需 Boltzmann 输运）、相变传热（需焓方法）、流体强制对流（需 CFD 耦合）。

## 2. 控制方程

傅里叶导热方程（瞬态）：

$$\rho c_p \frac{\partial T}{\partial t} = \nabla \cdot (k \nabla T) + Q$$

稳态（$\partial T/\partial t = 0$）退化为 Poisson 型方程：

$$-\nabla \cdot (k \nabla T) = Q$$

其中 $\rho$ 为密度（kg/m³），$c_p$ 为比热容（J/(kg·K)），$k$ 为导热系数（W/(m·K)），$Q$ 为体积热源密度（W/m³）。热流密度由傅里叶定律给出：

$$\mathbf{q} = -k \nabla T$$

## 3. 离散化方案

### 3.1 空间离散：有限体积法（FVM）/ Galerkin 有限元

**节点中心差分（FVM）**：节点 i 的控制体积 $\Delta V_i$，邻居 $j \in N(i)$，界面面积 $A_{ij}$，间距 $d_{ij}$，界面导热系数取调和平均保证通量连续：

$$k_{ij} = \frac{2 k_i k_j}{k_i + k_j}$$

**Galerkin 有限元**：以形函数 $N_i$ 展开温度 $T = \sum_i T_i N_i$，对控制方程做加权积分（权函数取 $N_i$）得弱形式：

$$\int_\Omega k \nabla N_i \cdot \nabla T \, d\Omega - \int_\Gamma N_i q_n \, d\Gamma = \int_\Omega N_i Q \, d\Omega$$

### 3.2 时间离散：隐式 Euler / Crank-Nicolson

隐式 Euler（无条件稳定）：

$$\rho c_p \frac{T^{n+1}_i - T^n_i}{\Delta t} = \frac{k_{i+1/2}(T^{n+1}_{i+1}-T^{n+1}_i) - k_{i-1/2}(T^{n+1}_i - T^{n+1}_{i-1})}{\Delta x^2} + Q_i$$

Crank-Nicolson（二阶时间精度，$\theta=0.5$）：

$$\mathbf{M} \frac{\mathbf{T}^{n+1} - \mathbf{T}^n}{\Delta t} + \mathbf{K}[(1-\theta)\mathbf{T}^n + \theta \mathbf{T}^{n+1}] = \mathbf{Q}$$

### 3.3 多维扩展

2D/3D 采用稀疏矩阵直接/迭代求解（`scipy.sparse.linalg.spsolve` 或 `gmres`），不引入 ADI 分裂（保证各向异性/非均匀网格精度）。

## 4. 边界条件

5 类标准边界（COMSOL Heat Transfer Module / Lumerical HEAT 共同支持）：

1. **Dirichlet（第一类，固定温度）**：$T|_{\Gamma} = T_0$（如恒温基底 300 K）。
2. **Neumann（第二类，固定热流）**：$-k \partial T/\partial n|_{\Gamma} = q_0$（绝热时 $q_0=0$）。
3. **Robin（第三类，对流）**：$-k \partial T/\partial n|_{\Gamma} = h(T - T_{\infty})$，$h$ 为对流换热系数（W/(m²·K)）。
4. **Radiation（辐射）**：$-k \partial T/\partial n|_{\Gamma} = \varepsilon \sigma (T^4 - T_{\infty}^4)$，$\varepsilon$ 为发射率，$\sigma = 5.67 \times 10^{-8}$ W/(m²·K⁴) 为 Stefan-Boltzmann 常数。辐射项非线性，需 Newton 迭代线性化。
5. **Contact（接触热阻）**：$-k \partial T/\partial n|_{\Gamma} = (T - T_{contact})/R_{th}$，$R_{th}$ 为界面热阻（m²·K/W）。

## 5. 核心算法逻辑（完整伪代码）

```
ALGORITHM HEAT_Solve(mesh, materials, sources, bcs, mode, dt, t_end):
  # 输入：
  #   mesh     = 节点坐标 + 单元连接表 + 控制体积 ΔV_i
  #   materials= 每区域 (ρ, c_p, k(T))，k 可温度依赖
  #   sources  = 热源列表 [(region, Q_type, value)]
  #              Q_type ∈ {VOLUME, JOULE, OPTICAL_ABSORPTION}
  #   bcs      = 边界条件列表 [(boundary, type, params)]
  #              type ∈ {DIRICHLET, NEUMANN, ROBIN, RADIATION, CONTACT}
  #   mode     = STEADY / TRANSIENT
  #   dt, t_end= 时间步长与终止时间（仅 TRANSIENT）
  # 输出：
  #   T[N] 节点温度场，q[N] 节点热流密度

  # === 步骤 1：网格生成与控制体积计算 ===
  N = mesh.num_nodes
  ΔV = compute_control_v