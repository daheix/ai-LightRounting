# PoLaRIS 用户手册

## 概述

PoLaRIS（光弈）是面向光电子芯片的 AI 自动布局布线引擎，支持 SOI / SiN / InP / LNOI 四大工艺平台。
本手册介绍如何使用 PoLaRIS 完成光子电路的自动布局、布线、仿真与训练。

## 快速开始

### 1. 最小 MZI 电路布局布线

```python
from polaris.data.specs import CircuitSpec, DeviceSpec
from polaris.pipeline.integrated import IntegratedPipeline, PipelineConfig

# 定义 MZI 电路：gc1 → mmi1 → gc2
circuit = CircuitSpec(
    name="minimal_mzi",
    devices=[
        DeviceSpec(name="gc1", device_type="grating_coupler", width_um=10.0, height_um=10.0),
        DeviceSpec(name="mmi1", device_type="mmi_1x2", width_um=20.0, height_um=10.0),
        DeviceSpec(name="gc2", device_type="grating_coupler", width_um=10.0, height_um=10.0),
    ],
    connections=[
        ("gc1", "o1", "mmi1", "o1"),
        ("mmi1", "o2", "gc2", "o1"),
    ],
    canvas_w=200.0,
    canvas_h=200.0,
)

# 运行端到端流水线
pipeline = IntegratedPipeline(PipelineConfig())
result = pipeline.run(circuit)
print(f"布局布线完成，损耗: {result.total_loss_db:.2f} dB")
```

### 2. 端到端流水线演示

```bash
python publish/examples/02_pipeline_e2e.py
```

### 3. PPO 训练快速入门

```bash
python publish/examples/03_ppo_training.py
```

### 4. PDK 器件库查询

```bash
python publish/examples/04_pdk_catalog.py
```

## 核心功能

### 布局引擎

布局引擎使用强化学习（PPO）自动放置光子器件：

```python
from polaris.engine.floorplan_env import FloorplanEnv, FloorplanEnvConfig
from polaris.engine.netlist import load_netlist

net, devices, _ = load_netlist("data/benchmarks/mzi.json")
env = FloorplanEnv(net, devices, config=FloorplanEnvConfig(
    canvas_w=200.0,
    canvas_h=200.0,
    grid_size=20.0,
))
obs, _ = env.reset()
```

### 布线引擎

布线引擎使用 A* 算法 + 拥塞感知排序自动连接波导：

```python
from polaris.router.waveguide_router import WaveguideRouter

router = WaveguideRouter(
    placements=env.state.placements,
    netlist=net,
    canvas_w=200.0,
    canvas_h=200.0,
)
routes = router.route_all()
```

### 仿真系统

仿真系统提供 S 参数级联与约束检查：

```python
from polaris.sim.simulator import Simulator
from polaris.sim.constraint_checker import check_constraints

sim = Simulator()
result = sim.simulate(circuit)
violations = check_constraints(result.layout)
```

### 训练框架

PPO 训练框架支持离散/连续动作空间：

```python
from polaris.trainer.ppo_torch import PPOAgent, PPOConfig, Transition
import numpy as np

agent = PPOAgent(
    obs_dim=113,
    action_dim=3,
    config=PPOConfig(lr=3e-4, n_epochs=4, batch_size=64),
    hidden_dim=128,
)

obs = np.random.randn(113)
for _ in range(64):
    action, logprob, value = agent.get_action(obs)
    agent.store(Transition(obs, action, -1.0, logprob, value, False))

metrics = agent.update(last_value=0.0)
print(f"policy_loss: {metrics['policy_loss']:.4f}")
```

### 专家奖励塑形

ICLR'26 专家知识注入 RL 奖励函数：

```python
from polaris.trainer.reward_shaping import ExpertRewardShaper, ExpertRewardInput

shaper = ExpertRewardShaper()
result = shaper.compute(ExpertRewardInput(
    device_positions={"a": (0.0, 0.0), "b": (100.0, 0.0)},
    connections=[("a", "p1", "b", "p2")],
))
print(f"专家奖励: {result.total_expert_reward:.3f}")
```

## 完整训练

使用 `train_2m.py` 进行 2M 轮持续 RL 训练（支持断点续训）：

```bash
# 启动训练（自动从上次断点恢复）
python scripts/train_2m.py

# 查看训练进度
cat checkpoints/rl_2m/progress.json | python -m json.tool | head -10
```

## PDK 器件库

PoLaRIS 支持四大工艺平台的器件模型：

| 平台 | 典型器件 | 用途 |
|------|----------|------|
| SOI | 波导/MMI/DC/Y分支/环/光栅耦合器 | 硅光集成 |
| SiN | 低损耗波导/低对比度器件 | 传感/低损耗 |
| InP | 有源器件/调制器/激光器 | 光源/高速调制 |
| LNOI | 铌酸锂波导/电光调制器 | 高速/低损耗 |

```python
from polaris.pdk.soi import SOI_CATALOG
from polaris.pdk.sin import SIN_CATALOG
from polaris.pdk.inp import INP_CATALOG
from polaris.pdk.lnoi import LNOI_CATALOG

# 查询 SOI 平台器件
print(SOI_CATALOG.list_devices())
```

## 仿真校准

对比自研仿真与基准数据，验证仿真精度：

```python
from polaris.sim.calibration import calibrate, CalibrationConfig

result = calibrate(CalibrationConfig(
    benchmark_dir="data/benchmarks",
    loss_tolerance_db=0.5,
))
print(f"校准: {result.passed_items}/{result.total_items} 通过")
print(f"最大误差: {result.max_error_db:.2f} dB")
```

## 来源

- PPO: Schulman et al., 2017, https://arxiv.org/abs/1707.06347
- ICLR'26 Expertise-Enhanced RL: https://openreview.net/forum?id=yqvNwfxRR6
- SiEPIC EBeam PDK: https://github.com/SiEPIC/SiEPIC_EBeam_PDK
